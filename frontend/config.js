window.RESUME_AI_CONFIG = {
    // After deploying the backend on Render, replace this with your Render URL.
    // Example: "https://ai-resume-analyzer-api.onrender.com"
    API_BASE_URL: "http://localhost:8080"
};
